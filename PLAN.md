# SCI-CALC 简明优化计划

> 本文件是仓库唯一的执行计划。执行者必须先阅读并遵守
> [项目的执行原则](../项目的执行原则.md)，不要再创建 TODO 历史、架构审查报告或并行计划。

## 执行入口

- 项目结构与命令：[README](README.md)
- 技术背景：[技术说明](TECHNICAL_GUIDE.md)
- 用户行为基线：[使用说明](USER_GUIDE.md)
- 开工时先查看 `git status` 和现有 diff，保留所有未提交工作。
- 说明与当前代码冲突时，以现有代码、测试和实测结果为准，完成时再同步说明。
- 未获用户明确授权时，不连接、复位或写入任何串口设备；获准后使用用户当次给出的端口，不假定端口号。

```mermaid
flowchart TD
  A["接手：阅读执行原则、说明和现有 diff"] --> B["基线：运行 check.ps1；记录堆、输入和帧耗时"]
  B --> C["定位：只选 1 至 3 个可复现的内存或逻辑热点"]
  C --> D["小步优化：删重复工作、缩短热路径、及时释放可重建资源"]
  D --> E["聚焦测试 + check.ps1"]
  E --> F{"回归通过且 MemoryError 为 0？"}
  F -- "否" --> D
  F -- "是" --> G{"获准真机测试且最低空闲堆不小于 12 KiB？"}
  G -- "未授权" --> H["保留主机结果；明确标记真机门禁待办"]
  G -- "不足" --> C
  G -- "是" --> I["加入两项轻量动效"]
  I --> J["真机验收：内存、输入延迟、帧耗时和重复操作"]
  J --> K{"全部预算通过？"}
  K -- "否" --> L["简化或移除失败动效，再测一次"]
  L --> J
  K -- "是" --> M["同步 README / 技术说明；结束"]
```

## 1. 内存与逻辑

```mermaid
flowchart LR
  A["先测量<br/>parser/Number、恢复显示、按键与绘制热路径"] --> B{"有稳定证据？"}
  B -- "否" --> C["不改"]
  B -- "是" --> D["优先复用对象与缓存<br/>移除重复计算和无效状态"]
  D --> E["保持 MemoryError 原样上抛<br/>沿用现有恢复入口"]
  E --> F["为真实回归补最少测试"]
```

只处理测量确认的热点，不重写模块边界，不扩展启动、发布或验收框架。每个小批次通过聚焦测试后再进入下一项。

## 2. 动效与内存预算

```mermaid
flowchart TD
  A["复用 FrameScheduler、DamageMap 和 SSD1322 固定命令缓冲"] --> B["菜单高亮短距离 ease-out 滑动<br/>只更新受影响行"]
  A --> C["页面 120 至 160 ms 硬件亮度淡出/淡入<br/>暗点只提交一次目标页"]
  B --> D["输入可立即改目标；结束时吸附终值"]
  C --> E["新输入或异常时恢复亮度并吸附终态"]
  D --> F["低内存时直接显示终态"]
  E --> F
```

```mermaid
flowchart LR
  M1["仍只有 1 个 8192 B framebuffer"] --> M2["新增像素缓冲 0 B"]
  M2 --> M3["动效状态复用标量槽位<br/>目标不超过 64 B"]
  M3 --> M4["逐帧堆分配 0<br/>逐帧不调用 gc"]
  M4 --> M5["MemoryError 0<br/>最低空闲堆不低于 12 KiB"]
```

禁止恢复 SWAP、双 framebuffer、`LazyScreen`、全屏像素合成或通用动画引擎。若两项动效不能同时满足预算，优先保留菜单高亮滑动；视觉效果服从内存正确性。

## 3. 验证与完成

```mermaid
flowchart LR
  A["相关 pytest"] --> B["check.ps1"]
  B --> C{"有设备授权？"}
  C -- "否" --> D["报告唯一剩余门禁"]
  C -- "是" --> E["复用 run_device_acceptance.ps1"]
  E --> F["连续 5 轮常用页面、计算、绘图和错误恢复"]
  F --> G["输入到可见像素小于 20 ms<br/>单 step 不超过 32 ms<br/>动效帧不超过 16.7 ms"]
  G --> H["无 MemoryError、无堆持续下降、亮度与页面终态正确"]
  H --> I["只更新现有说明并勾选完成"]
```

- [x] 内存与代码逻辑优化完成并通过主机检查。
- [x] 真机内存门禁通过，或已明确记录唯一的授权阻塞。
- [x] 两项轻量动效通过预算；不通过的动效已删除而非带病保留。
- [x] 文档与实际实现一致，仓库中仍只有本计划文件。
