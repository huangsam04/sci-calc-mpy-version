# SCI-CALC 1.4.0 执行树

这是唯一的主树干。接手时先读[项目的执行原则](../项目的执行原则.md)、本文件，以及当前任务对应的一个分支；不要再读取或创建历史 TODO、架构报告或并行计划。代码、测试和实际测量优先于说明。

## 当前边界

- 目标版本：`1.4.0`。
- 现有成果已在 `7cff5aa` 检查点保存；保留其删除记录和所有已有实现，不恢复旧动画、`LazyScreen`、SWAP、双 framebuffer 或旧计划。
- 设备 `COM5` 已获用户授权。部署或验收结束后让 OLED 回到硬件休眠，避免静态画面长期显示。
- 始终只有一个 8192 B framebuffer；新增像素缓冲为 0 B；热路径不调用 `gc.collect()` 或 `gc.mem_free()`；稳态逐帧堆分配为 0。
- 本版本不需要兼容未发布的旧应用状态或旧交互实现，但不为此重写与本任务无关的启动、发布或 A/B 部署逻辑。

## 分支

- [x] **入口修复**：[导航分支](plan/navigation.md)。已用主机回归修复 Calculator 和 Function Panel 的入口分发。
- [x] **绘图反馈**：[绘图分支](plan/plot-feedback.md)。在方程提交后显示真实进度，不增加像素存储。
- [ ] **内存、动效与版本**：[动效分支](plan/motion.md)。先测量和清理热点；只有真机最小空闲堆达到 12 KiB 才启用两项既定动效，并把版本升级到 `1.4.0`。
- [ ] **验证与说明**：[验证分支](plan/verification.md)。运行现有检查和 COM5 统一验收，再同步 README、TECHNICAL_GUIDE 和 USER_GUIDE。

## 执行顺序

1. 运行现有主机基线，并把可复现故障和测量结果记录到对应分支。
2. 先完成入口修复，再完成绘图反馈；每个小批次只跑相关测试。
3. 用真机数据选择至多三个稳定热点并处理。动效门禁未通过时，删除或保持禁用动效，不降低阈值。
4. 阶段完成后运行 `check.ps1`；最终只复用 `tools/run_device_acceptance.ps1 -Port COM5`。

完成一个分支时，必须在这里和该分支同时勾选；不要另建进度日志。
