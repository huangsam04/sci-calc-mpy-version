# SCI-CALC 冻结模块、按需页面与动效执行树

这是唯一的主树干。接手时先完整阅读[项目的执行原则](../项目的执行原则.md)、[权限牢笼](权限牢笼.md)、本文件，以及当前步骤对应的一个分支；不要读取或恢复旧 TODO、架构报告、`ARCHITECTURE_REVIEW.html` 或已删除计划。代码、测试和真机测量优先于说明。所有操作即使获得更宽泛的系统权限，也不得超出权限牢笼。

## 已知基线与边界

- 上一阶段检查点为 `88dd5c4`，应用版本为 `1.4.0`；保留现有成果及删除记录，不覆盖无关修改，不自行推送。
- COM5 已获连接、复位、写入和部署授权。不得整片擦除 Flash 或 SD 卡；必须保留用户 Add-ons、设置、变量和其他不可复现文件。每次设备操作结束后让 OLED 回到硬件休眠。
- 当前正式四模块 frozen 候选在 `calculator_history` 的最低空闲堆为 `15920 B`，阻塞 step 为 `37.724 ms`；内存已达到 `12288 B`，但时延仍超过 `32 ms`，因此两项动效均未启用且门槛不得降低。
- 始终只有一个 8192 B GS4 framebuffer；新增像素缓冲为 0 B；动效状态不超过 64 B；稳态逐帧分配为 0；按键和逐帧路径不调用 `gc.collect()` 或 `gc.mem_free()`。
- 保留 4-bit 灰阶、Calculator、Plot、Function Panel、Stopwatch、Settings 和现有用户功能。不得恢复 `LazyScreen`、SWAP、双 framebuffer、全屏像素合成或旧动画引擎。
- 本轮只为冻结模块、按需页面和两项动效改变启动/部署路径；不借机重建通用页面框架、验收系统或事务发布体系。

## 当前分支

- [x] **冻结核心模块**：[冻结固件分支](plan/frozen-firmware.md)。让稳定字节码和不可变常量从 Flash 直接使用，并以真机数据确认净堆收益。
- [x] **页面按需加载与彻底卸载**：[页面生命周期分支](plan/page-lifecycle.md)。由 `Nav` 独占页面创建、引用清理和失败回滚，只保留当前所需页面实例。
- [x] **两项动效**：[动效分支](plan/motion.md)。12 KiB/32 ms 真机门禁未同时通过，已按降级规则保持两项均不启用且不保留实验实现。
- [x] **剩余常驻模块冻结**：[冻结固件分支](plan/frozen-firmware.md#12-kib-续行剩余常驻生产模块)。`main` 因与 MicroPython 固定启动入口同名而被证据否决；保留 `performance`、`runtime_handle`、`version` 和 `approot` 后，最终 COM5 `heap_min=15920 B`、`MemoryError=0`，但 `blocking_max_us=37724`，因此继续下一分支。
- [x] **Calculator 直接求值**：[动效分支](plan/motion.md#12-kib-续行calculator-直接求值)。与固定历史组合后只把 `error_lifecycle` 改善到 `11136 B / 56.665 ms`，仍未过门；实现和专属测试已删除，Plot/Calculator 原接口已恢复。
- [x] **用户状态压缩**：[页面生命周期分支](plan/page-lifecycle.md#12-kib-续行无损用户状态压缩)。动态与固定 flat Calculator 历史都未满足联合门禁并已删除；Stopwatch 在当前失败场景之后，按 YAGNI 不实现。
- [x] **最终验证与说明**：[验证分支](plan/verification.md)。主机检查通过；COM5 统一验收按合同在首轮时延门禁停止，说明已同步、载荷已删除且 OLED 已休眠。

## 唯一执行顺序

1. 运行 `check.ps1` 和现有 COM5 验收，记录未修改基线并让 OLED 休眠。
2. 只执行冻结固件分支；每次只比较一个冻结集合，保留有稳定真机收益且无回归的最小集合。
3. 冻结结果稳定后执行页面生命周期分支；先按需创建和销毁实例，再处理仍有测量收益的模块运行态。
4. 用最大用户状态和现有连续五轮场景重新测量。只有最低空闲堆达到 12 KiB，才进入动效分支。
5. 每个小批次只运行相关测试；每个阶段结束运行一次 `check.ps1`；最终只扩展并复用现有 `tools/run_device_acceptance.ps1 -Port COM5`。

## 12 KiB 门禁续行（2026-07-31 用户新授权）

旧动效阶段的 1--3 个小热点上限已经按当时范围收口；本节是用户明确新增的后续授权，不恢复任何已否决实现。严格按上述三个新增分支顺序执行，每次只保留真机证明有净收益且不破坏行为、OOM 恢复、用户数据或 32 ms 门槛的候选。完整 application matrix 一旦同时达到 `heap_min >= 12288 B`、`blocking_max_us <= 32000`、`MemoryError=0`、普通错误 0，就停止尚未必要的后续重构并回到动效门禁；未达到时才进入下一项。所有 COM5 操作结束后清理临时载荷并休眠 OLED。

每完成一个分支，必须先在对应分支记录实际数据，再在这里勾选。不要创建其他计划、进度日志、ADR 或总结文件。
