# SCI-CALC 简明使用说明

这份说明适用于 SCI-CALC 1.4.0，面向只想把题目算出来的人。设备不需要联网；开机后从主菜单选择 `Calculator` 即可。

## 最快的用法

1. 用数字键和运算键输入公式，例如 `2+3*4`。
2. 按 `ENT` 计算。
3. 结果会留在下方历史记录中，可以继续引用。

常见例子：

```text
2+3*4          得到 14
2^3^2          得到 512
sqrt(9)+ln(1)  得到 3
max(3,5,4)     得到 5
```

请把乘号写出来：`2*x` 是正确写法，`2x` 不会自动理解成乘法。

## 长公式怎么输入

计算器顶部默认是单行公式编辑区，不再只显示开头的一小段。第一行放不下时，它会自动扩展成两行。

- 公式最多可输入 96 个字符；右下角的 `n/96` 显示当前已输入的字符数。
- 文字会按屏幕宽度自动换到第二行。短公式保持单行，让下方多显示一条历史记录；继续输入或移动光标时，屏幕会自动跟着光标显示当前部分。
- 右侧出现 `^` 或 `v` 时，表示上方或下方还有公式内容。按住 `Shift` 再按 `4` / `6` 可把光标向左 / 向右移动，检查或修改任意位置。
- `DEL` 删除光标左边的一个字符；长按会连续删除。`ESC` 先清空当前公式，空公式时才返回主菜单。

例如，先输入：

```text
(12.5+sqrt(81))*sin(30)+max(3,5,4)^2
```

公式较长时不需要重新抄写。继续输入即可，输入区会从一行展开到两行，光标所在的内容会留在屏幕中；移动光标就能回看前面的部分。

## 常用按键

| 按键 | 作用 |
| --- | --- |
| `ENT` | 计算当前公式。按住 `Shift` 再按 `ENT` 输入 `=`，可给变量赋值。 |
| `Tab` | 在输入和历史记录之间切换。 |
| `Shift+Tab` | 打开变量表。 |
| `RPN` | 打开函数选择器。 |
| `Shift+RPN` | 打开字母面板，输入变量名或函数名。 |
| `ANG` | 在角度 `DEG` 和弧度 `RAD` 之间切换。 |
| `ESC` | 清空当前公式；空公式时返回。长按一秒直接返回。 |

函数键会自动补上左括号。例如按 `sin` 会输入 `sin(`，再补上参数和右括号即可。

## 用历史记录继续算

按 `Tab` 进入历史记录后：

- `8` / `2` 选择上一条 / 下一条；
- `ENT` 把选中记录的结果接到当前公式后面；
- `4` / `6` 把选中记录的原公式接到当前公式后面；
- 再按 `Tab` 回到输入区。

历史会保留最近 20 次计算。单行输入时屏幕显示四条；输入区展开成两行时显示三条，选中项会高亮。

## 很大或很小的结果

结果以科学记数法显示，例如 `1.0000*10^1000`，因此很大的有限数不会显示成 `inf`。在
`Settings` 的 `Display digits` 项，可用 `4` / `6` 调整尾数小数点后的位数（1--12 位）；
这只改变显示，不会重新计算或截断已经保存的结果。

## 保存数值到变量

输入 `x=7` 后按 `ENT`，以后就可以在公式里写 `2*x+1`。变量表可以插入或删除变量。

变量名区分大小写：`x` 和 `X` 是两个不同的变量。设备会尝试保存变量；若屏幕提示 `Not saved - check SD`，请检查 SD 卡后等待提示消失，再断电。

## 绘制函数

从主菜单进入 `Plot`，按 `ENT` 编辑表达式，例如 `sin(x)` 或 `x^2`，再按 `ENT` 提交。
等待期间会显示 `Plotting` 和真实进度条；此时按任意操作键会取消旧绘图并执行该操作。

- 查看时用 `8` / `2` 缩放 Y；按住 `Shift` 再用 `8` / `2` 缩放 X。
- 用物理 `4` / `6` 左右平移；按 `RPN` 可快速输入 `x`。
- 编辑时按 `ESC` 放弃修改，按 `Shift+Tab` 把 X 范围恢复为 `-10` 到 `10`。

## 开关函数和插件

从主菜单进入 `Function Panel`。用 `8` / `2` 选择项目，按 `ENT` 开关函数组或 `Add-on`；
按 `ESC` 保存改动并返回。依赖项会自动启用，损坏或缺失依赖的插件会显示错误，可将其关闭后返回。

## 遇到报错时

屏幕会指出常见问题，例如除以零、括号没配对、变量不存在或函数参数不够。按任意键关闭提示，修改公式后再试。

更多函数、插件、刷入固件和维护说明见 [固件 README](README.md)。
